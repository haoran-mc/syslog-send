creating docker image via Dockerfile.

```
docker build -f Dockerfile -t syslog-send .
```

docker-compose creates containers from images.

docker-compose.yml is used to configure the parameters for creating containers.
